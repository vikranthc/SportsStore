@model IEnumerable<string>

@Html.ActionLink("Home", "List", "Product")

@foreach (var linkText in Model)
{
    @Html.RouteLink(linkText, new { controller = "Product", action = "List", category = linkText, page = 1 }, new { @class = linkText == ViewBag.SelectedCategory ? "selected" : null })
}
